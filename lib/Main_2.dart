import 'package:flutter/material.dart';
import 'camera.dart';
import 'firstpage.dart';

void main(){
  runApp(
    MaterialApp(
      title: 'trying to open camera',
      home: FirstPage() ,
    )
  );
}